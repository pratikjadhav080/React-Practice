const { expect } = require("@jest/globals");
const taxCal = require("../tax");

test("tax calc", () => {
  expect(taxCal(20000)).toBe(0);
});

test("tax calc", () => {
  expect(taxCal(20000)).toBe(0);
});

test("tax calc", () => {
  expect(taxCal(20000)).toBe(0);
});

test("tax calc", () => {
  expect(taxCalulator(20000)).toBe(0);
});

test("tax calc", () => {
  expect(taxCal(20000)).toBe(0);
});

test("tax calc", () => {
  expect(taxCal(20000)).toBe(0);
});




